import {api}from"./client";export type SalesOrder={id:number;orderNumber:string;orderDate:string;status:"DRAFT"|"CONFIRMED"|"CANCELLED";total:string;subtotal:string;taxTotal:string;customer:{id:number;name:string};items?:Array<{id:number;productSku:string;productName:string;quantity:string;unitPrice:string;taxRate:string;lineTotal:string}>;_count?:{items:number}};export const getSalesOrders=async(params:{status?:string;search?:string;page:number;pageSize:number})=>(await api.get<{data:SalesOrder[];meta:{page:number;totalPages:number;total:number}}>("/sales-orders",{params})).data;export const getSalesOrder=async(id:number)=>(await api.get<{data:SalesOrder}>(`/sales-orders/${id}`)).data.data;
export const setSalesOrderStatus=async({id,status}:{id:number;status:"CONFIRMED"|"CANCELLED"})=>(await api.patch<{data:SalesOrder}>(`/sales-orders/${id}/status`,{status})).data.data;
export const createSalesOrder=async(input:{customerId:number;orderDate?:string;notes?:string|null;items:Array<{productId:number;quantity:number;unitPrice?:number;taxRate?:number}>})=>(await api.post<{data:SalesOrder}>("/sales-orders",input)).data.data;

