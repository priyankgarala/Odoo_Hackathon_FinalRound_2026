import { createTheme } from "@mui/material/styles";
export const theme = createTheme({ palette: { primary: { main: "#176B62", dark: "#104B45" }, background: { default: "#F6F8F7" } }, shape: { borderRadius: 10 }, typography: { fontFamily: "Inter, Roboto, Arial, sans-serif" } });
