import { useState } from "react"; import { useNavigate } from "react-router-dom"; import { Alert,Box,Button,FormControl,IconButton,InputLabel,MenuItem,Paper,Select,Stack,Table,TableBody,TableCell,TableHead,TableRow,TextField,Typography } from "@mui/material"; import DeleteOutlineIcon from "@mui/icons-material/DeleteOutline"; import AddIcon from "@mui/icons-material/Add"; import { useMutation,useQuery } from "@tanstack/react-query"; import * as poApi from "../api/purchase-orders.api"; import { getContacts } from "../api/contacts.api"; import { getProducts } from "../api/products.api";
type Row={productId:string;quantity:number;unitPrice:string;taxRate:number}; const money=(v:number)=>new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR"}).format(v);
export const CreatePurchaseOrderPage=()=>{const nav=useNavigate();const[vendorId,setVendorId]=useState("");const[notes,setNotes]=useState("");const[rows,setRows]=useState<Row[]>([{productId:"",quantity:1,unitPrice:"",taxRate:0}]);const vendors=useQuery({queryKey:["po-vendors"],queryFn:()=>getContacts({type:"VENDOR",active:"true",page:1,pageSize:100})});const products=useQuery({queryKey:["po-products"],queryFn:()=>getProducts({active:"true",page:1,pageSize:100})});const save=useMutation({mutationFn:()=>poApi.createPurchaseOrder({vendorId:Number(vendorId),notes:notes||null,items:rows.map(r=>({productId:Number(r.productId),quantity:r.quantity,unitPrice:r.unitPrice?Number(r.unitPrice):undefined,taxRate:r.taxRate}))}),onSuccess:o=>nav(`/purchase-orders/${o.id}`)});const update=(i:number,p:Partial<Row>)=>setRows(rows.map((r,n)=>n===i?{...r,...p}:r));const total=rows.reduce((s,r)=>{const p=Number(r.unitPrice)||Number(products.data?.data.find(x=>x.id===Number(r.productId))?.unitPrice)||0;return s+p*r.quantity*(1+r.taxRate/100)},0);return <Stack spacing={3}><Box><Typography variant="h4" fontWeight={750}>Create Purchase Order</Typography><Typography color="text.secondary">Draft vendor order with price snapshots.</Typography></Box>{save.isError&&<Alert severity="error">Could not create purchase order. Check all fields.</Alert>}<Paper sx={{p:3}}><Stack spacing={3}><FormControl fullWidth><InputLabel>Vendor</InputLabel><Select label="Vendor" value={vendorId} onChange={e=>setVendorId(e.target.value)}><MenuItem value="">Select active vendor</MenuItem>{vendors.data?.data.map(v=><MenuItem key={v.id} value={String(v.id)}>{v.name}</MenuItem>)}</Select></FormControl><TextField label="Notes" value={notes} onChange={e=>setNotes(e.target.value)} multiline minRows={2}/><Table><TableHead><TableRow><TableCell>Product</TableCell><TableCell>Qty</TableCell><TableCell>Unit price</TableCell><TableCell>Tax %</TableCell><TableCell/></TableRow></TableHead><TableBody>{rows.map((r,i)=><TableRow key={i}><TableCell><FormControl size="small" fullWidth><Select value={r.productId} displayEmpty onChange={e=>update(i,{productId:e.target.value,unitPrice:""})}><MenuItem value="">Select product</MenuItem>{products.data?.data.map(p=><MenuItem key={p.id} value={String(p.id)}>{p.sku} · {p.name}</MenuItem>)}</Select></FormControl></TableCell><TableCell><TextField size="small" type="number" value={r.quantity} onChange={e=>update(i,{quantity:Number(e.target.value)})}/></TableCell><TableCell><TextField size="small" type="number" value={r.unitPrice} placeholder="Product price" onChange={e=>update(i,{unitPrice:e.target.value})}/></TableCell><TableCell><TextField size="small" type="number" value={r.taxRate} onChange={e=>update(i,{taxRate:Number(e.target.value)})}/></TableCell><TableCell><IconButton disabled={rows.length===1} onClick={()=>setRows(rows.filter((_,n)=>n!==i))}><DeleteOutlineIcon/></IconButton></TableCell></TableRow>)}</TableBody></Table><Button startIcon={<AddIcon/>} onClick={()=>setRows([...rows,{productId:"",quantity:1,unitPrice:"",taxRate:0}])}>Add item</Button><Typography align="right" variant="h6">Total: {money(total)}</Typography><Stack direction="row" justifyContent="flex-end" gap={1}><Button onClick={()=>nav("/purchase-orders")}>Cancel</Button><Button variant="contained" disabled={!vendorId||rows.some(r=>!r.productId||r.quantity<=0)||save.isPending} onClick={()=>save.mutate()}>Save draft purchase order</Button></Stack></Stack></Paper></Stack>};
