import { useMemo,useState } from "react"; import { Link as RouterLink } from "react-router-dom"; import { Box,Button,Chip,FormControl,InputLabel,MenuItem,Pagination,Paper,Select,Stack,Table,TableBody,TableCell,TableContainer,TableHead,TableRow,TextField,Typography } from "@mui/material"; import { useQuery } from "@tanstack/react-query"; import * as api from "../api/purchase-orders.api"; import { EmptyState } from "../components/feedback/EmptyState"; import { ErrorState } from "../components/feedback/ErrorState"; import { LoadingState } from "../components/feedback/LoadingState";
const money=(v:string)=>new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR"}).format(Number(v));
export const PurchaseOrdersPage=()=>{const[search,setSearch]=useState("");const[status,setStatus]=useState("");const[page,setPage]=useState(1);const params=useMemo(()=>({search:search||undefined,status:status||undefined,page,pageSize:20}),[search,status,page]);const orders=useQuery({queryKey:["purchase-orders",params],queryFn:()=>api.getPurchaseOrders(params)});return <Stack spacing={3}><Stack direction="row" justifyContent="space-between" alignItems="center"><Box><Typography variant="h4" fontWeight={750}>Purchase Orders</Typography><Typography color="text.secondary">Vendor purchasing workflow with price and tax snapshots.</Typography></Box><Button component={RouterLink} to="/purchase-orders/new" variant="contained">Create purchase order</Button></Stack><Paper sx={{p:2}}><Stack direction={{xs:"column",md:"row"}} gap={2}><TextField label="Search order or vendor" value={search} onChange={e=>{setSearch(e.target.value);setPage(1)}} fullWidth/><FormControl sx={{minWidth:170}}><InputLabel>Status</InputLabel><Select label="Status" value={status} onChange={e=>{setStatus(e.target.value);setPage(1)}}><MenuItem value="">All statuses</MenuItem><MenuItem value="DRAFT">Draft</MenuItem><MenuItem value="CONFIRMED">Confirmed</MenuItem><MenuItem value="CANCELLED">Cancelled</MenuItem></Select></FormControl></Stack></Paper>{orders.isLoading?<LoadingState label="Loading purchase orders..."/>:orders.isError?<ErrorState message="Could not load purchase orders." onRetry={()=>void orders.refetch()}/>:orders.data!.data.length===0?<EmptyState message="No purchase orders match your filters."/>:<><TableContainer component={Paper}><Table><TableHead><TableRow><TableCell>Order</TableCell><TableCell>Date</TableCell><TableCell>Vendor</TableCell><TableCell>Items</TableCell><TableCell align="right">Total</TableCell><TableCell>Status</TableCell></TableRow></TableHead><TableBody>{orders.data!.data.map(o=><TableRow key={o.id} hover component={RouterLink} to={`/purchase-orders/${o.id}`} sx={{textDecoration:"none",cursor:"pointer"}}><TableCell><Typography color="primary" fontWeight={700}>{o.orderNumber}</Typography></TableCell><TableCell>{new Date(o.orderDate).toLocaleDateString()}</TableCell><TableCell>{o.vendor.name}</TableCell><TableCell>{o._count?.items}</TableCell><TableCell align="right">{money(o.total)}</TableCell><TableCell><Chip size="small" label={o.status} color={o.status==="CONFIRMED"?"success":o.status==="CANCELLED"?"default":"warning"}/></TableCell></TableRow>)}</TableBody></Table></TableContainer><Box sx={{display:"flex",justifyContent:"flex-end"}}><Pagination page={page} count={Math.max(1,orders.data!.meta.totalPages)} onChange={(_,v)=>setPage(v)}/></Box></>}</Stack>};
